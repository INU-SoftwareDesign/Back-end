from django.urls import path
from . import views

urlpatterns = [
    #path('<int:pk>/', views.ParentDetailView.as_view()),
    #path('<int:pk>/connect-child/', views.ConnectStudentView.as_view()),
]
